# Application config
spring.application.name=big_city_library-rest_client_public
server.port=8080

# Active profile
spring.profiles.active=dev
!spring.profiles.active=prod

# Spring console
spring.output.ansi.enabled=always
